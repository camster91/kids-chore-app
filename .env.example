# ===========================================
# Kids Chore App - Environment Configuration
# ===========================================

# Database - Use Supabase, Neon, or other PostgreSQL provider
# For production, always use ?sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# NextAuth - REQUIRED
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# NextAuth URL - Set to your production domain
NEXTAUTH_URL="https://yourdomain.com"

# Environment
NODE_ENV="production"

# ===========================================
# Hostinger Deployment Notes:
# ===========================================
# 1. Create PostgreSQL database (Supabase/Neon free tiers work great)
# 2. Set all variables above in Hostinger Node.js settings
# 3. Run: bash scripts/deploy.sh
# 4. Configure Hostinger to proxy to port 3000
